ADD R2, R3, R4


ADD = opcode
R2, R3, R4 = registers


let opcodebin = match opcode {
  opcodes binarys...
}

let regsbin = 0;

for reg in regs {
  let bin = match registers {
    registers bianrys...
  }

  regsbin = regsbin + bin;
}



write_on_memory = opcodebin+regsbin 



lc3assembly
 -> Handle the flow of parse lines, convert to binarys and store in the memory

instructions
 -> Handle the flow of fetch instructions from memory and execute.
